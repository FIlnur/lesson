"use strikt"; //strikt - использовать строго

// Площадь прямоугольника
const width = Number(5);
const heidht = Number(7.1)
const area = width*heidht;
console.log(area);

const logic = Boolean(true == false);
const str = String("hello");
const megastr = String('${width} * ${heidth} = ${area}');
console.log(megastr);
